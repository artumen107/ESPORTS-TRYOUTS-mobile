<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".Fragments.Games">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/gamesList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/buttonsGames"/>

    <LinearLayout
        android:id="@+id/buttonsGames"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/gamesList"
        app:layout_constraintBottom_toBottomOf="parent"
        app:flow_verticalAlign="center"
        app:flow_horizontalAlign="center"
        android:gravity="center">
        <Button
            android:id="@+id/buttonAddGame"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:text="+"
            android:textSize="48sp"
            android:layout_margin="10dp"/>

        <Button
            android:id="@+id/buttonBackGame"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:text="cofnij"
            android:textSize="24sp"
            android:layout_margin="10dp"/>
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>